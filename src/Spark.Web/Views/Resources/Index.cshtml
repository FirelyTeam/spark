@inject Spark.Web.Services.ServerMetadata ServerMeta

    <h1>Resources</h1>

    <ul class="list-group list-group-flush col-md-6">
        @foreach (var item in await ServerMeta.GetResourceStatsAsync())
        {
            <li class="list-group-item">
                @item.ResourceName
                <span class="badge rounded-pill text-bg-secondary float-end">@item.Count</span>
            </li>
        }
    </ul>
