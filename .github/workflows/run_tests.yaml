name: Tests

on:
  push:
    branches:
      - 'r4/master'
  pull_request:

jobs:
  build:
    if: github.repository == 'FirelyTeam/spark'

    runs-on: ${{ matrix.platform }}
    strategy:
      matrix:
        platform: [ windows-latest, ubuntu-24.04, macos-14, macos-14-large ]

    steps:
      - uses: actions/checkout@v6
      - name: Setup .NET Core
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.0.x
      - name: Unit tests
        run: |
          dotnet test "./Tests/Spark.Engine.R4.Tests/Spark.Engine.R4.Tests.csproj"
          dotnet test "./Tests/Spark.Mongo.Tests/Spark.Mongo.Tests.csproj"
          dotnet test "./Tests/Spark.Web.Tests/Spark.Web.Tests.csproj"
